<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - API Security Scanner</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    body {
      padding-top: 20px;
      padding-bottom: 40px;
    }
    .navbar {
      margin-bottom: 20px;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .card {
      margin-bottom: 20px;
    }
    .drag-area {
      border: 2px dashed #ccc;
      border-radius: 5px;
      padding: 25px;
      text-align: center;
      cursor: pointer;
      transition: border 0.3s ease;
    }
    .drag-area.active {
      border-color: #007bff;
    }
    .drag-area i {
      font-size: 50px;
      color: #ccc;
    }
    .drag-area h3 {
      margin: 15px 0;
      color: #666;
    }
    .drag-area p {
      color: #888;
    }
    .file-info {
      display: none;
      margin-top: 15px;
      padding: 10px;
      background-color: #f8f9fa;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-light rounded">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">API Security Scanner</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link {{#if (eq activeTab 'home')}}active{{/if}}" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link {{#if (eq activeTab 'scan')}}active{{/if}}" href="/scan">Scan Target</a>
            </li>
            <li class="nav-item">
              <a class="nav-link {{#if (eq activeTab 'dashboard')}}active{{/if}}" href="/dashboard">
                <i class="bi bi-speedometer2"></i> Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link {{#if (eq activeTab 'admin-prompts')}}active{{/if}}" href="/admin/prompts">
                <i class="bi bi-gear"></i> LLM Prompts
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link {{#if (eq activeTab 'mcp-admin')}}active{{/if}}" href="/admin/mcp">
                <i class="bi bi-database-gear"></i> MCP Admin
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    {{{body}}}

    <footer class="mt-5 pt-3 text-muted border-top">
      <p class="text-center">&copy; 2025 API Security Scanner</p>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Helper function for handlebars
    document.addEventListener('DOMContentLoaded', function() {
      // Add any global JavaScript here
    });
  </script>
</body>
</html>
